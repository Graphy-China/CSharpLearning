<%= form_for @event, :url => { :controller => 'events', :action => 'update', :id => @event } do |f|  %>

<!--  without local template.
    <%= f.label      :name, "Name" %>
    <%= f.text_field :name %>

    <%= f.label      :description, "Description" %>
    <%= f.text_area  :description %>
-->
    <%= render :partial => 'form', :locals{:f => f} %>

    <%= f.submit 'Update' %>
<% end %>
